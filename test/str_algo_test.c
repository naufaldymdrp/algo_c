#include <stdio.h>
#include <assert.h>
#include <string.h>

#include "str/str_algo.h"

int main(int argc, char** argv) {
    int result = 1;
    assert(result == 1);
    if (result == 0) return 0;
    else return -1;
}
